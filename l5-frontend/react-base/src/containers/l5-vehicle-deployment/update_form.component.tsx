import React from "react";

function UpdateForm(props: any ) {
    if (props.allowed == false) {
        return (<></>);
    } else {
        return (
            <></>
        );

    }
}
export default UpdateForm;